使用C99编写获取macos所有进程列表的函数，并且能够通过进程id获得可执行文件名称,
并且能够通过进程id获得启动时的命令行参数。
并包装为V语言函数，并编写V的单元测试
在每个文件最后显示个总行数。验证函数也要写为C函数。不需要构建脚本。
使用no body func, 加 @[importc: "name"], 让V封装代码更少。
包名为 psutil，C函数加上当前包名前缀 psutil_。
但是源代码文件名加上 _macos后缀。
example 写在 单元测试文件中，注释和测试输出使用英文。
